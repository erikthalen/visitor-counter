const second = 1000
const minute = second * 60
const hour = minute * 60
const day = hour * 24
const month = day * 30.415
const year = month * 12

export const time = {
  second,
  minute,
  hour,
  day,
  month,
  year,
}
